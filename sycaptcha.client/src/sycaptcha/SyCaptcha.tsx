import { CaptchaBar } from '@components';

/**
* SyCaptcha es el componente para la verificación de usuarios humanos.
* 
* @returns Un componente de React que renderiza el captcha.
*/
export const SyCaptcha = () => <CaptchaBar />
